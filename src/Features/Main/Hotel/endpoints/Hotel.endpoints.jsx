import { BASEURL } from "../../constants/api.constant"
      
      export const HotelEndpoints = {
          FIND: `${BASEURL}/Hotel/find`,
          UPDATE: `${BASEURL}/Hotel/update`,
          SEND: `${BASEURL}/Hotel/send`,
      }
      