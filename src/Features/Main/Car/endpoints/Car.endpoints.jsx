import { BASEURL } from "../../constants/api.constant"
      
      export const CarEndpoints = {
          FIND: `${BASEURL}/Car/find`,
          UPDATE: `${BASEURL}/Car/update`,
          SEND: `${BASEURL}/Car/send`,
      }
      