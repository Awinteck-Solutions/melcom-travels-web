import { BASEURL } from "../../constants/api.constant"
      
      export const FlightEndpoints = {
          FIND: `${BASEURL}/Flight/find`,
          UPDATE: `${BASEURL}/Flight/update`,
          SEND: `${BASEURL}/Flight/send`,
      }
      